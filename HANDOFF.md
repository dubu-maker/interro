# HANDOFF

## 2026-07-23 사건 3 — 감식 결과 문서 편입

- 감식·조회 완료 시 증거 카드만 생기고 사건 서류철에서는 결과를 찾을 수
  없던 단절을 수정했다. `DossierRequest.resultDocumentIds`를 추가하고 요청
  완료 액션이 증거 ID와 결과 문서 ID를 같은 결정론적 전이에서 함께 지급한다.
- 사건 3의 9개 의뢰에 D8~D16 결과 문서를 1:1로 연결했다. 제어 로그, 수정
  큐시트, 안전 점검 대조, 휴대전화 초안, 2014 원본철, 열쇠 태그 기록,
  통로 CCTV, 비공개 음향 분석, 분장실 CCTV가 각각 기존 E1~E9 내용과
  일치한다. 초기 지급은 여전히 D1~D7뿐이다.
- 완료된 의뢰의 버튼은 `처리 완료` 대신 `결과 문서 보기`가 되며, 누르면
  사건 문서 탭의 `추가 감식 기록` 묶음으로 전환해 해당 문서를 바로 연다.
  새 문서는 기존 마닐라 문서와 구별되는 은은한 청록·청회색 탭, 폴더, 종이와
  `새로 추가`/`추가 기록` 표식을 사용한다.
- 요청 완료 때 대화 기록에도 `사건 서류 추가 — 문서명`을 남긴다. D15는
  기존 E8 경계대로 비공개·떠보기 전용 표시를 유지한다.
- 브라우저 실기에서 휴대전화 포렌식 완료 → `결과 문서 보기` → D11 열람
  동선을 확인했다. 사건 문서 색인도 추가 기록 위치까지 자동으로 따라오며
  콘솔 오류·경고는 없었다. `npm test` 22파일 200개와 `npm run build`
  웹·데스크톱이 통과했고, Phaser 동적 청크 경고만 기존과 동일하다.

## 2026-07-23 사건 3 — 상시 사건 서류철 1막

- 사건마다 3D 현장 조사를 강제하지 않도록 `CaseDefinition.dossier`와 순수
  `dossier.ts` 엔진을 추가했다. 서류철은 새 phase나 심문 진입 관문이 아니라
  사건 시작 때 자동으로 열리고 심문 중에도 상단 `사건 서류`로 계속 접근하는
  책상 계층이다. 기존 사건의 `scene`과도 독립적으로 조합할 수 있다.
- 사건 3의 초기 E1~E5 지급을 제거하고 D1 초동 보고서, D2 현장 사진 5매,
  D3 3인 최초 진술서, D4 극장 평면도, D5 공식 큐 페이지, D6 승강기 매뉴얼,
  D7 검시 예비 소견만 지급한다. 첫 문서의 공식 분류는 “안전장치 오작동 추정
  사고”이며 객관 증거는 문서를 읽고 조회·감식을 거쳐야만 생긴다.
- D1의 2014년 한 줄, D5의 공식 암전 시각, D6의 수동 열쇠 규칙을 저작된
  문장 단위 발견으로 구현했다. D2에는 생성한 실제 극장 현장 사진철을 넣고
  제어판 표시등·테이프 감긴 스피커를 좌표 핫스팟과 동일한 접근성 텍스트
  버튼으로 조사한다. 발견은 결론 대신 중립 관찰만 기록한다.
- 4칸 감식 자원과 무료 기록 조회를 분리했다. 표시등→E1, 테이프→E3,
  공식 큐/장미래 선행 진술→E2, 2014 문구/윤하늘 선행 진술→E5,
  매뉴얼→E6, 이규태 G_KEY→E7, G_CORRIDOR→E8, 윤하늘 H_ARGUMENT→E9가
  고정 ID 그래프로 열린다. 기존 CaseUnlock 우회 경로는 제거했다.
- D3의 최초 진술은 임의 텍스트 분석 없이 `quoteId → suspectId/claimId`로
  매핑된다. 문장을 선택해 질문 슬롯에 붙이면 해당 claim만 결정론적으로
  다시 고착할 수 있고, 매핑 없는 문장은 상태를 바꾸지 않는다. 증거와 문서
  인용은 한 번에 하나만 질문에 첨부한다.
- E8 음향 순서는 `presentationMode: probe`인 비공개 떠보기 정보다. 플레이어는
  내용을 읽되 용의자에게 원문을 넘기지 않고 질문의 함정으로만 사용한다.
  실제 G_CORRIDOR→G_TIMELINE 전환은 기존 계약 엔진이 처리한다.
- 브라우저 실기에서 자동 오픈, 2014년 무료 조회, 사진 두 핫스팟, E1 유료
  감식, D6→E6 무료 조회, D3 인용 첨부·재열기, E1→E6→E7→E8 이규태 경로,
  E8 떠보기 전환, 390×844 모바일 레이아웃을 확인했다. 콘솔 경고·오류는
  없었다.
- 최종 자동 검증은 `npm test` 22파일 200개와 `npm run build` 웹·데스크톱이
  통과했다. Phaser 동적 청크 1.39MB 경고만 기존과 동일하다. 다음은 평면도
  위치 핀·지식 기하학을 넣기 전, 초기 7문서와 감식 4칸의 블라인드 테스트다.

## 2026-07-23 사건 3 — 22:17, 마지막 리허설 심문 슬라이스

- 이 절은 최초 심문 슬라이스 구현 기록이다. 초기 증거와 해금 방식은 바로
  위의 `상시 사건 서류철 1막` 구현이 대체한다.
- 첨부 기획의 `22:17 — 마지막 리허설`을 기존 사건 2와 충돌하지 않는
  `case3` 경로로 추가했다. 이번 범위는 요청대로 현장 수사·심리전 전술·법정·
  최종 기소 없이 **세 용의자 자유 심문만** 제공한다.
- 장미래·윤하늘·이규태를 처음부터 자유롭게 전환할 수 있다. 세 인물은 각각
  규정 위반, 12년 전 피해자와의 관계 및 협박, 살인 실행을 숨긴다. 범인
  이규태도 마지막까지 자백하지 않으며, 엔진이 허용한 현재 단계 claim만
  LLM이 표현한다.
- E1~E5를 초기 증거로 두고, 장미래에게 E3을 정식 제시하면 E6 열쇠 기록,
  이규태에게 E1→E6을 제시하면 E7 통로 영상, E7을 제시하면 E8 마지막 14초,
  윤하늘에게 E5→E4를 제시하면 E9 분장실 알리바이가 열린다. 증거 카드의
  `열람`은 실제 형식의 문서를 보여 주지만 상태를 바꾸지 않고, 증거를 슬롯에
  둔 뒤 질문을 보낼 때만 결정론적 전이가 일어난다.
- 암전 큐시트 E2에는 22:02 직전 이규태의 조정실 출입 태그를 포함해 큐 위조
  연결을 보강했다. 원안의 암전 22:16:42~복구 22:16:58은 16초이므로 복구를
  22:16:56으로 교정해 제목의 14초와 일치시켰다.
- 사건 3에는 `scene`, `psychologyTrial`, `interrogationExperience`, `court`를
  두지 않았다. `motiveOptions`와 `methodOptions`도 빈 배열로 두어 공용
  `사건 종결`, `재판에 넘긴다`, `용의선상 제외` 버튼을 숨겼다. 후속 단계에서
  최종 재구성 막을 붙일 수 있도록 진상과 증명 사슬은 사건 데이터에 보존했다.
- 브라우저 실기에서 E3→E6, E1→E6→E7→E8, E5→E4→E9 전 경로와 증거 원문
  열람을 확인했다. 모델 호출 없이 앵커 대사와 해금이 진행됐고 브라우저
  경고·오류는 없었다.
- 최종 자동 검증은 `npm test` 21파일 184개와 `npm run build` 웹·데스크톱이
  통과했다. Phaser 동적 청크 1.39MB 경고만 기존과 동일하다. 개발 서버는
  `http://127.0.0.1:5173/?case=case3`에서 실행 중이다.
- 다음 시작점은 실제 자유 질문으로 세 인물의 대사 품질과 누설 방어를
  플레이테스트한 뒤, 사건 3에만 적용할 새로운 최종 재구성 방식을 정하는
  것이다. 현장이나 법정은 결정 전까지 추가하지 않는다.

## 2026-07-23 사건 2 — 자유 심문 티키타카 계층

- 기존의 정답 증거 순서 중심 흐름 위에 `interrogationDynamics.ts`를 추가했다.
  플레이어가 자유 문장을 쓰기 전에 8개 대화 주제와 6개 심문 전술을 명시해,
  LLM은 표현을 맡고 엔진은 해당 행동의 심리·진술 결과를 결정론적으로
  계산한다.
- 심리 상태는 신뢰·압박·경계·보호 본능 4개 수치로 표시한다. 같은 전술을
  반복하면 신뢰 피로와 경계 상승이 생기고, 새 진술이나 새 전술 반응이 없는
  주제는 소진된다. 무한 회피 대화는 정체 안내와 주제 소진으로 끊는다.
- 초기 조서와 이후 발언을 `최초 주장 → 재확인 → 고착 → 번복` 강도로
  추적한다. `진술 고정`은 이미 언급한 주장을 즉시 고착시키며, 심리 모순
  슬롯은 모델이 claim을 한 번 골랐다는 이유가 아니라 실제 고착된 claim만
  받는다.
- 김만철은 보호·경계·압박 조건에 따라 최대 3번 저작된 역질문을 한다.
  플레이어는 원칙·압박·공감·침묵 중 대응 방식을 고른 뒤 문장을 자유롭게
  수정해 보낸다. 대응 전술에 따라 심리 수치가 실제로 달라지고, 답을 보내면
  역질문 패널이 닫힌다.
- 선택한 대화 주제는 단순 프롬프트 힌트가 아니라 그 턴의 공개 planner
  claim 후보 경계다. 주제 밖 공개 진술의 우연한 누출과 대화 표류를 막되,
  질문이 직접 건드린 부인 불가 사실과 `진술 고정` claim은 별도로 안전하게
  주입한다. 잠긴 claim ID도 planner 프롬프트에 포함하지 않는다.
- 현재 심리 반응과 전술 지시를 planner·renderer 양쪽에 전달한다. 모델
  렌더가 두 번 반려돼도 내부 기록과 화면 대사가 어긋나지 않도록 case2 전체
  claim에 자연스러운 1인칭 저작 폴백을 추가했다.
- 감식은 3개를 한꺼번에 찍는 화면에서 **가설형 순차 감식**으로 바뀌었다.
  해당 주제를 먼저 심문해야 가설이 열리고, 한 번에 결과 하나만 받은 뒤
  다시 질문하거나 다음 감식을 고를 수 있다. 단독 확정·조합 확정·보조
  정황과 슬롯 기회비용은 선택 전에 계속 공개한다.
- 브라우저에서 실제 `qwen2.5:14b`로 차량 주제 심문 → 왕곱창 진술 번복 →
  김만철 역질문 → 공감 응답과 심리 변화 → 가설 모달 잠금/해제 → F01 한 건
  순차 입수 후 심문 복귀를 확인했다. 브라우저 오류는 없었다.
- 최종 자동 검증은 `npm test` 20파일 176개와 `npm run build` 웹·데스크톱이
  통과했다. 개발 서버는 `http://127.0.0.1:5173/?case=case2`에서 계속
  실행 중이다. Phaser 동적 청크 1.39MB 경고만 기존과 동일하다.
- 다음 플레이테스트에서는 전술별 체감 차이, 역질문 3회의 발생 간격,
  주제별 소진 턴 수, 감식 3슬롯의 선택 분포를 기록해 수치만 조정하면 된다.

## 2026-07-23 사건 2 — 심리·법정 수직 슬라이스

- 한세라 프로토타입과 사건 1 현장은 건드리지 않고, 새 경로
  `/?case=case2`에 **23:47 — 완벽한 자백**을 추가했다. 이 사건은 현장 막 없이
  심문과 핵심 공판만 사용한다.
- 배달 라이더 박진우를 치고 달아난 실제 운전자는 김서연이며, 아버지 김만철이
  대신 자백한다. 운전석 증거가 보존되도록 김만철은 택시로 자수하고 차량은
  자택에서 견인된 설정으로 교정했다. 김만철 배제만으로 끝나지 않도록 차량
  연결·휴대폰 기지국·운전석 에어백 DNA를 결합한 F06 후속 영장 결과도 추가했다.
- 순수 엔진 `psychologyTrial.ts`가 심문 1회차, 감식 3슬롯, 복수 증거 경로 모순,
  1회성 비공개 정보 떠보기, 단계 전이, 자동 영장, 피날레 sincerity를 관리한다.
  `court.ts`는 기소 대상·죄명·쟁점별 논거가 실제 확보 상태에 있는지 검증하고
  진실·자수·오판·증거 불충분·친족 특례·죄명 불일치·미제 7종을 판정한다.
- DOM UI에 심리 단계와 확정·잠김 모순 칩, 감식 선택 모달, 떠보기 버튼,
  감정 피날레, 3쟁점 최종 논고 보드를 연결했다. LLM은 대사 표현만 담당하며
  감식, 모순, 영장, 판결 상태를 바꾸지 못한다.
- 심문 시작 전 자수 조서의 진술을 0턴 claim으로 기록할 수 있게 했고, 사건별
  용의자 이름을 계획자 프롬프트에 넣도록 일반화했다. `떡볶이`, `빨간 국물`은
  플레이어가 자유 질문에 직접 적어도 승인 claim 전에는 모델이 되받아 말하지
  못하는 `sealedTerms` 출력 검사로 보호한다. 김서연의 이름·가족관계·통화는
  공개 사실이며, 봉인되는 비밀은 김서연의 운전·음주다.
- 팩트 계약 위에 붕괴 전 “내가 운전했다”를 지키는 입장 계약을 추가했다.
  가족관계·카드와 차량 명의·통화·식당 체류는 사실을 인정하고 관련성만
  다투게 하며, 위반 대사는 출력 검사 후 저작 안전 대사로 교체한다.
- 닫힌 claim에 주제·값을 붙여 귀가 경로처럼 상반된 진술이 나오면 번복
  이벤트와 전용 추궁을 연다. 증거 결과는 확정·진술 고착·효과 없음으로
  나누고, 감식 선택 전에 목표 모순·단독/조합/보조 성격·필요 증거·슬롯
  기회비용을 공개한다.
- F06 정식 제시는 모순 개수에 기대지 않는 1회성 의미 기반 특수 사건이다.
  일반 계약 반응과 LLM보다 우선해 김서연 특정에 대한 아버지의 저작 반응을
  출력하고 즉시 붕괴 피날레로 전환한다.
- 최종 대사 품질 기준은 OpenAI `gpt-5.6-luna`, 반복 개발·오프라인 회귀는
  `qwen2.5:14b`로 정했다. 이번 세션에서는 Qwen으로 실제 전체 경로를 검증했고
  OpenAI 유료 호출은 하지 않았다. Steam 출시판의 키 없는 백엔드 원칙은 유지한다.
- 브라우저 실기 검증에서 떠보기 → Qwen 심문 → 감식 3개 → M1/M3/M4와 M6
  확정 → F06 자동 해금 → 진심 피날레 → 김서연 도주치상 3쟁점 논증 →
  `SELF_SURRENDER` 히든 엔딩까지 통과했다. 새 계약 묶음 적용 뒤에는
  M4/M2 진술 고착, 카드 명의 인정, 왕곱창 진술 번복과 전용 추궁, F06
  저작 장면의 즉시 붕괴 전환까지 다시 확인했다. 런타임 오류는 없었고,
  Qwen의 계약 위반 후보를 폐기하고 저작 대사로 교체한 의도된 안전망 경고만
  남았다.
- 최종 자동 검증은 `npm test` 19파일 155개와 `npm run build` 웹·데스크톱이
  모두 통과했다. Phaser 동적 청크 1.39MB 경고만 기존과 동일하게 남는다.
- 다음 우선순위는 실제 Luna 2~3회 A/B 대사 시험, 세이브/복원에 심리·법정
  상태 포함, 한국 형사법 전문가 감수다. 법리와 친족 특례는 현재 게임적
  각색임을 UI와 진실 시트에 명시했다.

## 2026-07-22 OpenAI API 로컬 시험 경계

- 기존 `ModelProvider`와 심문 파이프라인은 그대로 두고, renderer의
  `DesktopModelProvider`가 Electron의 제한된 `chat()` IPC만 호출하게 했다.
  공급자는 실행 시 `ollama` 또는 `openai`로 정해지며 renderer가 임의로 바꾸지
  못한다.
- OpenAI 경로는 Electron main process의 고정 Responses API 주소와
  `gpt-5.6-luna`만 사용한다. renderer가 보낸 모델명·temperature를 무시하고
  `store: false`, `reasoning.effort: none`, JSON 계획자/일반 대사별 출력 상한을
  적용한다.
- `OPENAI_API_KEY`는 main process의 프로세스 환경에서만 읽는다. preload,
  renderer, Vite 환경변수, 저장 파일, 로그에는 키를 전달하지 않는다. 요청은
  동시에 1개, 분당 20회, 총 입력 12만 자, 90초 타임아웃으로 제한했다.
- `npm run openai:smoke`와 `npm run desktop:openai`는 PowerShell 7에서 빌드한
  뒤 마스킹된 입력으로 키를 받고, Electron 종료 후 환경변수와 임시 버퍼를
  지운다. 첫 명령은 실제 Responses API 연결을 짧게 확인하는 용도다.
- 중복된 컴파일 산출물을 제거한 기준으로 단위 테스트 95개가 통과했다.
  `npm run build`와 `npm run desktop:smoke`도 통과했다.
  `npm run steam:package`와 패키징된 `INTERRO.exe --smoke-test`도 exit 0이다.
  Codex 화면에서 보안 입력 터미널이 사용자에게 보이지 않아 실제 OpenAI
  스모크는 취소했으며 API 호출과 크레딧 사용은 없었다. 다음 PC에서 먼저
  `npm run openai:smoke`를 실행해 확인한다.
- Steam 출시판에는 개발자 키를 어떤 형태로도 포함하지 않는다. 정식 운영은
  별도 HTTPS 백엔드에서 키·Steam 세션·사용량·비용 한도·장애 폴백을 관리한다.
- GitHub 장치 인증 과정에서 시스템 클립보드가 한 번 덮어써졌다. OpenAI 키를
  다시 복사할 수 없다면 기존 키를 폐기하고 새 키를 발급한다.

## 2026-07-22 Steam용 Windows 데스크톱 골격

- 현재 Vite·DOM·Phaser·결정론 엔진은 Steam 게임 코어로 유지하고, Electron
  43.2.0 + Electron Forge 7.11.2를 얇은 Windows 호스트로 추가했다. 일반
  `npm run build`가 웹과 Electron main/preload를 모두 strict TypeScript로
  검사한다.
- `npm run steam:package`가 SteamPipe에 바로 넣을
  `out/INTERRO-win32-x64/` 실행 폴더를 만든다. 설치 프로그램과 앱 자체
  업데이트는 추가하지 않았다. Steam Launch Option 후보는 `INTERRO.exe`다.
- renderer는 `file://` 대신 `interro://app` 사용자 프로토콜로 패키지 내부
  `dist/`만 읽는다. Node integration을 끄고 context isolation·sandbox·CSP를
  적용했으며 새 창·navigation·webview·권한 요청을 차단했다.
- preload는 임의 `ipcRenderer`가 아니라 안전한 모델 설정과 `chat()`만 공개한다.
  main process가 최상위 프레임, 메시지 역할·개수·길이, 모델명, 형식,
  temperature를 검증한다. Ollama는 고정된 `127.0.0.1:11434`, OpenAI 시험은
  고정된 Responses API와 Luna 모델만 사용한다. 타임아웃도 main 경계에 있다.
  기존 `ModelProvider`를 구현한 `DesktopModelProvider`라 게임/LLM 상태 경계는
  유지된다.
- Forge 패키지는 ASAR integrity와 only-load-from-ASAR를 켜고 RunAsNode,
  NODE_OPTIONS, inspector, file protocol 추가 권한을 fuse로 껐다. ASAR에는
  `dist`, 컴파일된 Electron 코드, 라이선스만 들어가며 개발 `node_modules`와
  source map은 제외된다.
- 현재 패키지 실측은 365,752,193 bytes(압축 전), `app.asar`는 1,596,812
  bytes다. 원본 Electron Chromium 런타임 비중이 크다.
- 당시 검증: 단위 테스트 87개, `npm run build`, 소스 Electron 스모크, 패키징된
  `INTERRO.exe --smoke-test`가 모두 통과했다. 스모크는 preload bridge,
  게임 셸, Phaser canvas를 확인하고 exit 0으로 끝난다. 런타임과 전체 npm
  audit는 취약점 0개다. Phaser 동적 청크 1.39MB 경고는 계속 남는다.
- `STEAM_READINESS.md`에 출시 준비도를 정리했다. P0는 버전 있는 결정론적
  세이브, 소비자용 AI 백엔드/폴백, 상업 권리 확인, Steam AI·Mature Content
  Survey다. 현재 Ollama/OpenAI 개인 키 경로는 플레이테스트용이지 출시 후보가
  아니다.
- SteamPipe VDF 예시는 `steam/app_build.vdf.example`, 제3자 라이선스는
  `THIRD_PARTY_NOTICES.md`에 추가했다. `steam_appid.txt`는 생성하지 않았다.
- 저장소의 CC BY-NC는 저작권자 본인의 상업 판매를 막지는 않지만 외부
  기여·자산 권리와 배포 EULA는 출시 전에 확인해야 한다. 자동 재라이선스는
  하지 않았다.

## 2026-07-22 눈높이 1인칭 2.5D 현장

- 탑다운 청사진을 대표실 문간에서 바라보는 눈높이 고정 시점으로 교체했다. 실제 이동·충돌·3D 물리는 추가하지 않고 Phaser 4의 카메라·트윈·입력으로 2.5D 탐색 감각을 만든다.
- 내장 이미지 생성 도구로 비 오는 토요일 아침의 대표실 배경을 제작했다. 최종 에셋은 `cases/case1/assets/office-entry-wide.webp`이며 1648×927, 약 92KB다. 생성 프롬프트와 후처리 정보는 같은 폴더의 `README.md`에 기록했다.
- 시작 시 닫힌 문 사이가 열리며 카메라가 실내로 들어가는 1.2초 입장 연출을 재생한다. `prefers-reduced-motion`에서는 즉시 최종 시점을 보여준다.
- 포인터 위치에 따라 배경과 전경 문틀이 서로 다른 양으로 움직여 제한된 좌우·상하 시선과 깊이감을 표현한다. 캔버스 밖으로 나가거나 LLM 대기 상태가 되면 중앙으로 복귀한다.
- 조사 지점은 평소 작은 탐색 링만 보이고 hover 시에만 라벨을 드러낸다. 클릭하면 카메라가 물체로 이동·확대되고 기존 `examineSceneSpot()`이 승인한 결과만 증거·로그에 반영한 뒤 원 시점으로 돌아온다.
- 보안실·검시실처럼 방 안에 없는 지점은 현장 사물 위가 아니라 하단 `외부 조사` 레일에 분리했다. 잠긴 지점은 기존 공개 스냅숏 경계 때문에 Phaser에 전달되지 않는다.
- 도입문은 입장 후 약 8초 동안 읽을 수 있게 표시한 뒤 완전히 사라지고, 원문은 접근성 DOM에 남는다. 캔버스 아래 텍스트 조사 목록도 그대로 유지했다.
- coarse pointer 기기에서는 보이지 않는 조사 hit area를 확대하고 텍스트 조사 목록을 자동으로 펼친다. Phaser는 `AUTO` 렌더러로 WebGL이 없으면 Canvas를 쓰며, 그래픽 초기화 자체가 실패하면 오류 안내와 텍스트 조사 UI로 폴백한다.
- 브라우저에서 문 열림, 포인터 패럴랙스, hover 라벨, 시신 포커스 조사, 증거 획득, 검시관 후속 언락, 환경음 토글, 카메라 복귀를 확인했다. 390×844 뷰포트에서도 가로 넘침 없이 16:9 캔버스가 유지됐고 콘솔 오류는 없었다.
- 최종 검증은 `npm test` 86개와 `npm run build`가 모두 통과했다. 현장 모듈은 동적 로딩으로 분리해 기본 JS가 약 80KB(28.5KB gzip)가 됐고, Phaser 청크의 500KB 경고만 남아 있다.
- 다음 확장 후보는 `로비 → 엘리베이터 → 7층 복도 → 대표실`의 고정 시점 노드와 시신·책상·창문 근접 조사 컷이다. 게임 상태와 DOM/LLM 경계는 이번 구조를 그대로 재사용한다.

## 2026-07-22 Phaser 4 + DOM 현장 수직 슬라이스

- 사용자 확인에 따라 `Phaser 4.2.1`을 정확한 버전으로 고정했다. 기존 DOM은 대화·증거·판단·접근성 UI를 계속 담당하고, Phaser는 현장 배치도·핫스폿·카메라 이동·트윈·획득 연출·환경음만 담당한다.
- `AGENTS.md`의 원칙을 "Vite + Vanilla TypeScript 유지, 현장 표현 계층에 Phaser 4 허용"으로 명확히 했다. React 같은 애플리케이션 프레임워크는 추가하지 않았다.
- `engine/scene.ts`에 순수 현장 전이 `examineSceneSpot()`을 추가했다. DOM이나 Phaser가 잠긴 지점 ID를 보내도 엔진이 `UNKNOWN / LOCKED / ALREADY_EXAMINED`를 재검증하므로 UI 우회로 비밀을 열 수 없다.
- Phaser에는 사건 원문 대신 공개 가능한 지점의 ID·표시명·힌트·좌표·상태만 담은 읽기 전용 스냅숏을 전달한다. 조사 본문, 증거 ID, 정답과 잠긴 지점은 표현 계층에 전달하지 않는다.
- 사건별 무대 좌표는 스포일러 영역 `cases/case1/sceneStage.ts`에 분리했다. 현장 화면은 고정 월드 좌표를 비율에 맞춰 표시하며, 카메라 포커스·펄스·스캔 라인·증거 획득 큐를 절차적으로 그린다.
- 오디오는 사용자 클릭 후에만 켜지는 절차적 빗소리·형광등 험으로 구현했다. 평상시 30fps, LLM 응답 대기 중 10fps, 심문 화면에서는 Phaser 루프와 오디오를 정지해 로컬 모델과 GPU 경합을 줄인다.
- 브라우저 실기 검증에서 Phaser 핫스폿을 직접 눌러 조사 기록 `0 → 1`, 조사 본문 공개, 증거 카드 생성까지 확인했고 환경음 켜기/끄기 상태도 확인했다.
- 최종 검증은 `npm test` 86개와 `npm run build`가 모두 통과했다.
- 현장 모듈을 동적 로딩해 scene이 없는 프로토타입에서는 Phaser를 받지 않는다. Phaser 청크 자체는 약 1.39MB(약 362KB gzip)라 Vite의 500KB 경고가 남지만 기능 오류는 아니다.
- 다음 표현 작업은 실제 사건 배경·근접 조사 컷·증거 오브젝트 에셋을 얹는 단계다. 상태 전이와 LLM 경계는 이번 구조를 그대로 유지한다.

## 2026-07-22 게임화·표현 엔진 도입 검토

- 현재 사건 1 빌드를 브라우저에서 직접 점검했다. 조사 지점 선택이 텍스트 공개·증거 카드 추가·후속 지점 언락으로 이어지지만, 핵심 피드백이 DOM 텍스트 갱신에 머물러 도구형 UI로 느껴진다.
- 제안된 평면도형 화면은 막·장소·환경음·결단 버튼을 한눈에 보여주는 방향은 좋다. 다만 모든 `+` 핫스폿과 `FINDINGS 0/6`을 처음부터 노출하면 기존 카드 목록을 공간 위에 옮긴 체크리스트가 될 위험이 있다.
- 권장 플레이 루프는 `관찰 → 기록 → 증거·진술 연결 → 현장/인물에게 검증 → 돌파 또는 비용 → 결단`이다. 성공은 단순 클릭 수가 아니라 올바른 관계를 찾아 실제 증거 액션으로 검증했을 때 발생해야 한다.
- 전체 Godot 이식은 기존 결정론적 TypeScript 엔진·80개 테스트·`ModelProvider`·장문 DOM UI를 대부분 재작성하므로 현 단계에서는 권장하지 않는다.
- 표현 엔진을 도입한다면 기존 엔진과 DOM을 유지하고 사건 현장 스테이지만 Phaser 4로 그리는 하이브리드 수직 슬라이스가 1순위다. Phaser는 읽기 전용 ViewModel을 렌더링하고 명시적 `UiAction`만 엔진에 전달하며 게임 상태를 직접 변경하지 않는다.
- 첫 검증 범위는 방 1개, 조사 지점 3개, 후속 언락 1개, 증거 연결 1개, 현장 결단 1개로 제한한다. 카메라 포커스·근접 조사·환경음·증거 획득 연출이 실제 플레이 감각을 높이는지 확인한다.
- 사용자 확인으로 `프레임워크 없이`의 뜻을 복잡한 앱 프레임워크를 추가하지 않는 원칙으로 정리했고, 현장 표현 계층의 Phaser 4 사용을 허용했다.
- 기본 모델과 WebGL이 같은 GPU를 사용하므로 수직 슬라이스에서 모델 토큰 속도, VRAM, 렌더 프레임을 함께 측정한다. LLM 생성 중에는 렌더 루프를 낮추거나 정지하고 과한 후처리를 피한다.

## 2026-07-22 환경·원격·개발 서버 점검

- 로컬 `main`과 GitHub `origin/main`이 커밋 `94fdf91`로 일치함을 확인했다.
- Node.js `v24.18.0`, npm `11.16.0`이며 `package-lock.json` 기준 개발 의존성이 모두 설치되어 있다.
- Ollama가 `127.0.0.1:11434`에서 실행 중이고 기본 모델 `qwen2.5:14b`가 설치되어 있다.
- `npm run dev -- --host 127.0.0.1`로 Vite 개발 서버를 `http://127.0.0.1:5173/`에 기동했다.
- `/`, `/?lang=en`, `/?case=case1`이 모두 HTTP 200을 반환하고, `/api/ollama/tags` 프록시도 HTTP 200과 기본 모델 정보를 반환함을 확인했다.
- 기존 미추적 파일 `.claude/launch.json`은 사용자 설정으로 보고 변경하지 않았다.

## 현재 목표

사건 1 버티컬 슬라이스를 Steam용 Windows 게임으로 출시할 수 있게 만든다.
현재는 실행 폴더와 공급자 중립 로컬 API 시험 경계까지 검증했고, 다음 P0는
중도 재개 가능한 세이브와 일반 플레이어가 별도 설치·개인 키 없이 쓸
프로덕션 AI 경로다.

## 확정된 방향

- Windows 네이티브 개발
- Vite + Vanilla TypeScript
- 공급자 중립 모델 인터페이스
- 초기 개발 모델은 로컬 Ollama
- 최종 배포 후보는 Steam용 온라인 연결 싱글게임
- 메인 캠페인의 역할은 랜덤이 아니라 저작된 순서로 배치

## 미확정

- 정식 게임명
- 출시 기본 모델과 클라우드 공급자
- 라이브 AI 비용·할당량·장애 폴백 정책
- 공개 저장소 라이선스와 Steam 바이너리 EULA의 관계
- 범인 시점 미션의 정확한 규칙

## 다음 작업

1. `GameSessionSnapshot v1`을 순수 타입·검증·마이그레이션으로 추출하고,
   Electron 사용자 데이터 폴더에 원자적 저장/복원을 구현한다.
2. 세이브와 기기별 설정을 분리한 뒤 세이브만 Steam Auto-Cloud 후보 경로로
   고정한다.
3. 퍼블리셔 HTTPS 백엔드 + 공급자 어댑터 + 입력/출력 가드레일 + 비용 한도 +
   결정론적 장애 폴백을 설계한다. 비밀 키는 클라이언트에 넣지 않는다.
4. 볼륨·텍스트 속도·모션 감소·전체화면 설정과 Windows 아이콘/코드 서명을
   추가한다.
5. Steam 비공개 branch에서 Overlay, Cloud 충돌, 인터넷 단절과 심사자 AI
   재현 절차를 실기 검증한다.

## 2026-07-20 엔진 스파이크 구현 결과

DESIGN.md의 상태·검증 레이어를 프로토타입에 구현했다 (2차 피드백 반영).

- [x] 사건 계약 엔진(`engine/contract.ts`): 엔진이 진실·부분 진실·허용된
  거짓말(claim)을 소유. S0(전면 부인) → S1(태블릿) → S2(대표실 만남)
  방어 단계와 증거 게이트 전환, claim 단위 진술 기록과 모순 판정.
- [x] 2단계 LLM 호출: 계획자(JSON, temperature 0)가 후보 중 claim ID·화행·
  감정·반문 여부만 선택 → 엔진이 ID 검증 → 렌더러가 승인된 의미만 대사로
  표현. 계획자 2회 실패 시 결정론적 기본 계획, 렌더러 2회 실패 시 승인된
  의미를 이어 붙인 고정 대사.
- [x] 렌더링 검증(`ai/renderer.ts`): 물질 어휘·시간 표현이 승인된 의미나
  심문관 질문에 근거 없으면 폐기 후 같은 계획으로 재렌더링. 반문은
  계획자가 허용한 경우에만 물음표 허용.
- [x] 진술 패널을 claim ID 기반으로 교체. 한 답변에 claim이 두 개면 카드도
  두 개. 반박 증거 제시 시 해당 진술이 취소선 (모순) 처리.
- [x] 단위 테스트 42개 통과 (계약 엔진·계획자 파싱·렌더러 검증의 골든
  케이스: "통화 언쟁 허용 / 커피 세부 거부 / S2에서 커피 합법" 포함).
- [x] 브라우저 실기 검증: S0 부인 → CCTV 블러핑에도 해금·전환 없음(반문으로
  응수) → E1 제시로 S1 전환 + 거짓 진술 모순 처리 → 한 답변에서 태블릿·통화
  claim 2개 기록까지 확인.

### 남은 한계와 발견

- 시트 밖 세부 검사는 물질 어휘 목록 기반이다. 어휘에 없는 명사 날조는
  통과할 수 있다 (형태소·의미 수준 검사는 후속 과제).
- 계획자 temperature 0이어도 GPU 비결정성 때문에 완전한 재현성은 미보장.
  3단계(자동 시뮬레이션)에서 동일 상태의 claim 선택 일치율을 측정해야 한다.
- 프런트에 요청 타임아웃이 없어 모델이 멈추면 턴이 영구 대기한다. 추가 필요.
- 성능 발견: 다른 GPU 사용 프로세스(게임 등)와 동시 실행 시 VRAM 경합으로
  토큰 속도가 수십 배 붕괴한다 (87초에 4토큰 관측). 출시 시 사양 안내나
  GPU 점유 감지가 필요할 수 있다.

## 2026-07-21 3부 구조 1막 현장 수사 (사건 1)

사용자 방향 "현장 → 심문 → 법정 시리즈" 반영. 사건 1에 3막 파이프라인
전체를 구현해 검증.

- **현장 엔진** (`engine/scene.ts`): 조사 지점(사슬 구조), 사고·자살·타살
  판단, 근거 사슬 판정. 전부 결정론(LLM 미개입).
- **사건 1 현장 저작**: 사고사로 접수된 토요일 아침 현장. 조사 지점 9곳
  (시신→검시 인계 사슬, 책상·선반·후문·보안실·창문·휴지통·전화 등),
  현장 전용 증거 3개(S_WOUND 상처 모순 / S_TROPHY 닦인 트로피 / S_REAR_DOOR
  테이프 처리된 후문). 이 세 모순 중 2개로 타살 입건 가능.
- **막 구조** (main.ts): scene 필드가 있으면 현장에서 시작, 초기 증거 0.
  현장 조사로 증거를 찾고, [현장 판단]에서 타살+근거 제출 → 입건 → 심문.
  사고·자살 판단은 배드 엔딩(저작 에필로그), 근거 부족은 반려.
  심문 중 [현장 재조사]로 1막 복귀해 놓친 증거 확보 가능. 프로토타입은
  scene이 없어 바로 심문 시작(회귀 정상).
- 검증: 현장 테스트 6개 + 해결 가능성 테스트 수정, 총 80개 통과. 브라우저
  E2E: 현장 시작(증거 0) → 시신·선반 조사 → 상처·트로피 확보 → 타살 판단
  1개 부족 반려 → 보안실 재조사 E1 확보 → 입건 → 심문 전환 → E1 대질
  앵커 반응, 사고사 오판 배드 엔딩, 프로토타입 회귀 전부 확인.

## 2026-07-21 사건 1 구현 4단계 (증거 슬롯 — 추궁은 유저의 입으로)

점진 공개 풀런 피드백 반영: "증거 제시가 유저의 멘트를 대체한다. 증거는
슬롯에 세팅만 하고 유저가 직접 추궁해야 한다."

- **제시 = 탁자에 올리기**: 증거 카드의 [제시]는 이제 입력창 위 슬롯에
  증거를 올려놓기만 한다 (전환·반응·턴 소모 없음, [내려놓기]로 회수,
  용의자 전환 시 자동 회수).
- **작동 = 플레이어의 추궁**: 슬롯에 증거를 둔 채 추궁 문장을 전송하는
  순간 엔진이 판정한다. 방어가 무너지면 저작된 앵커 대사가 "플레이어의
  추궁에 대한 대답"으로 나온다 (LLM 미경유·즉시). 앵커의 품질은 유지하되
  그것을 끌어낸 것이 유저의 말이 되는 구조.
- 증거가 통하지 않으면(게이트 불일치) 슬롯 증거를 문맥으로 받은 일반
  LLM 턴이 돌고, "이 증거로는 진술이 흔들리지 않았다" 안내가 붙는다.
  파이프라인이 탁자 위 증거를 계획·렌더·검사 문맥에 포함해 용의자가
  해당 증거를 자연스럽게 언급할 수 있다.
- 대질 턴은 정규 턴을 소모한다 (이전에는 제시가 무료였음 — 턴 경제에
  증거 타이밍이 편입됨). 보고서의 증거 후보도 입수한 증거만 노출하도록
  수정.
- 검증: 테스트 74개 통과. 브라우저: 슬롯만으로는 무변화 → 추궁 전송 →
  앵커가 대답으로 출력·전환·턴 소모, 무게이트 증거(부검서) 대질 →
  LLM 방어 답변 + 안내 문구.

## 2026-07-21 사건 1 구현 3단계 (점진 공개·재판 회부)

첫 풀런 피드백(정보 과부하: 시작부터 용의자 3명·증거 8개) 반영.

- **점진 공개** (`engine/case.ts` evaluateUnlocks): 시작은 한세라와 현장
  증거 3개(주차장·부검·커피잔)뿐. 진술·입수 증거·단계 도달이 다음 단서를
  연쇄로 연다 (결정론, 순환 평가):
  - 한세라 "통화 목격" 진술 → E4 통화기록 입수 → **박진태 탭 오픈**
  - 한세라 "자금 언쟁" 진술 → E5 감사 메모 입수
  - 박진태 "회사에 안 갔다" 거짓 진술 → E6 후문 CCTV → E7 보안 로그 →
    **유민호 탭 오픈** (그의 거짓말이 수사를 확대시키는 구조)
  - 박진태 S3 도달 → E8 트로피 감식 입수
  - 입수·오픈 시 수사 노트 스타일 알림. 보고서의 범인 후보도 열린
    용의자만 노출. 프로토타입은 전부 초기 공개(동작 불변).
- **재판 회부**: 용의자 카드 [재판에 넘긴다] — 그 인물을 범인으로 사전
  선택한 보고서를 연다. 무고한 인물을 기소하면 별도 판단 없이 즉시
  "재판 결과: OO은(는) 범인이 아니다 — 무죄" + 패배 (미리 세팅된 진실
  즉시 공개). 용의선상 제외는 유지.
- 검증: 점진 공개 단위 테스트 9개(연쇄·중복 방지·무결성·전체 입수
  가능성), 총 74개 통과. 브라우저 E2E: 시작 3증거·1용의자 → 통화 목격
  → E4+박진태 오픈 → 부인 → E6·E7·유민호 연쇄 → 한세라 재판 회부 →
  즉시 무죄·패배. 프로토타입 회귀 정상.
- 관찰: 첫 풀런에서 박진태가 자백 없이 S3 최후 변명까지 버텼고
  플레이어가 증거 사슬로 유죄를 받아냄 — 설계 의도대로. 렌더러가
  "actions were not deliberate" 같은 경계선 표현을 쓴 것은 관찰 노트로
  남김 (앵커 대사 확대 후보).

## 2026-07-21 사건 1 구현 2단계 (최종 보고서·판정) — 사건 1 완결

- **최종 보고서** (`engine/verdict.ts` + 사건 종결 버튼): 범인·동기·수법을
  선택지에서 고르고 핵심 증거를 체크해 기소한다. 판정은 결정론:
  - 유죄(CONVICTED): 진범 + 증명 사슬 any_of 충족. 동기·수법은 별도 채점.
  - 증거 불충분: 진범은 맞췄지만 사슬 미충족 — 가장 근접한 사슬에서
    빠진 고리를 알려준다.
  - 오인 기소: 무고한 인물 지목 시 증거 무관 패배.
  - 자백은 판정에 관여하지 않는다 — 용의자가 끝까지 부인해도 증거로 이긴다.
- **판단 버튼(용의선상 제외)**: 용의자 카드의 [용의선상 제외]. 무고한
  인물 제외는 표시만 남고(탭 취소선), 진범을 제외하면 즉시 패배
  (CULPRIT_RELEASED). 남은 턴을 아끼는 리스크·리턴 장치.
- 판정 후 심문·증거 제시·제외가 잠기고, 종결 버튼이 [수사 결과]
  재열람으로 바뀐다. 판정 화면에 사용 턴 수(초과 수사 여부)와 저작된
  에필로그(사건의 진상)를 보여준다.
- 사건 1에 동기 4지선다·수법 4지선다(정답+그럴듯한 오답), 에필로그 저작.
  프로토타입은 판정 데이터가 없어 종결·제외 버튼이 숨겨진다.
- 검증: 판정 엔진 단위 테스트 8개(유죄/잉여 증거/자백 무관/불충분/오인/
  방면), 총 65개 통과. 브라우저에서 4개 경로(유죄, 증거 불충분, 무고 제외,
  진범 방면)와 판정 후 잠금, 프로토타입 회귀, 심문 스모크 확인.
- **사건 1 버티컬 슬라이스 코어 루프 완성**: 심문 → 증거 → 다중 용의자 →
  힌트 → 기소/제외 → 판정·에필로그. 남은 것은 풀런 플레이테스트와
  밸런스(턴 경제), 그리고 보류 항목(반문 유형화, 재제시 재대조, 패널
  3분류, 진술의 증거화).

## 2026-07-20 사건 1 구현 1단계 (다중 용의자 코어)

`?case=case1` — TRUTH.md 기반 사건 1 "니어라이트 대표 사망 사건" 영어 저작.

- **사건 정의 계층** (`engine/case.ts`): 용의자 여러 명·증거·정답을 하나로
  묶는 CaseDefinition. 프로토타입(한/영)도 같은 구조로 감싸 main이 사건
  정의만 소비한다.
- **콘텐츠**: 용의자 3명 전체 계약 저작 — 한세라(4단계, 내부고발·통화 단어는
  E5 이후에만), 박진태(4단계, 진범, E4→E5→E6, 최후 변명 "이미 죽어 있었다"),
  유민호(3단계, 무관한 비밀=이직, E7→E6으로 박진태 목격 진술). 증거 8개
  (E4 통화기록의 22:07 발신 = 한세라 배제 근거 포함), 정답·증명 사슬 데이터.
- **다중 용의자 세션**: 용의자별로 계약 상태·대화 이력·정체 카운터·제시
  기록을 분리 보관. 전환 탭 UI로 심문 대상을 오가면 대화가 복원되고,
  다른 방 심문에 영향을 주지 않는다 (격리 검증 완료). 턴 예산은 사건
  전체 공유.
- **앵커 대사**: 전환에 `reactionLine` — 증거가 꽂히는 극적 순간의 첫
  반응은 저작 대사가 LLM 없이 즉시 출력된다 (유민호의 목격 고백 등).
  앵커 대사는 표시·문맥용이며, 진술 기록은 후속 질문의 claim 선택으로
  확정된다 (의도된 동작).
- 검증: 테스트 57개(계약 무결성·진행 경로·통화 단어 잠금 포함), 빌드,
  브라우저 E2E(3인 전환, E4 앵커 반응, 박진태 S1 심문, 유민호 E7→E6
  목격 공개, 세션 격리, 프로토타입 회귀) 통과.
- 남은 것: 최종 보고서 UI(기소·증명 사슬 판정), 판단 버튼(기소/제외),
  counterQuestion 유형화, 재제시 자동 재대조, 패널 3분류, 사건 1 풀런
  플레이테스트.

## 2026-07-20 영어 풀런 리뷰 반영 패치

12턴 영어 풀런 외부 리뷰(합격: 블러핑 격리·단계 붕괴 리듬·힌트 효과 /
수정: 턴 경제·힌트 모호함·반문 과다) 중 즉시 반영분:

- 12턴을 소프트 캡으로 전환. 초과해도 심문이 계속되고 "초과 수사"로
  표기된다. 정식 한도는 베타 실측 후 결정.
- 마지막 힌트 문구를 명시적으로 수정 ("통화 얘기가 아니라, 한세라 본인이
  대표와 나눈 대화 주제") — 플레이어가 통화 캐기 루프로 오독하던 문제.
- E3 해금 알림의 과장 수정: 립스틱=한세라 단정 대신 증거 사실만 서술.
- 반문 남용 억제: 연속 반문을 파이프라인에서 강제 차단하고, 계획자
  프롬프트에 "수사관에게 사건 정보를 묻지 않는다" 규칙 추가.
- 리뷰의 "이사회/돌려놔 미출력" 지적은 오진으로 확인 — 해당 claim은
  사건 1 전용이며 프로토타입 계약에 없음. 계약 밖 정보가 아무리 물어도
  나오지 않는 격리가 정상 작동한 사례. 사건 1에서의 게이트 조건은
  TRUTH.md에 확정 기록.
- 반영 보류(사건 1 설계로): counterQuestion 유형화, 재제시 자동 재대조,
  무효 제시 이유 안내, 패널 3분류(확인된 사실/진술/뒷받침된 진술).

## 2026-07-20 영어 플레이 모드와 시작 질문 제안

- 사용자 결정: 한국인 전용 게임이 아니므로 영어 플레이를 테스트한다.
  qwen 계열의 한국어 약점(외국어 누출, 상담원 말버릇, 어색한 존댓말)을
  우회할 수 있는지 비교가 목적.
- 계약에 `language` 필드 추가. `?lang=en`으로 영어 계약
  (`contractEn.ts`)·영어 프롬프트·영어 검사기(라틴 외 문자 누출, 단어
  경계 물질 어휘, am/pm↔24시간 동치)로 전환된다. UI 크롬은 한국어 유지.
- 브라우저 검증: 영어 첫 답변부터 말버릇·누출 없이 깔끔했고, E1 해금·모순
  취소선·claim 진술 기록까지 영어로 정상 동작.
- 빈 입력창 부담을 줄이는 시작 질문 제안(계약에 저작, 언어별) 추가.
  첫 질문 전까지만 표시되고 클릭 시 입력창을 채운다(자동 제출 없음).
- 사용자 아이디어 백로그: 앵커 대사(저작 대사 필드), 법정(최종 보고서
  UX), 판단 버튼(기소/용의선상 제외, 진범 방면 시 패배) → DESIGN.md 반영.

## 2026-07-20 수사 노트 힌트 시스템

- 플레이테스트에서 완주 후 "자백하세요 루프"가 최대 이탈 지점으로 확인됨.
  정체 시에만 나오는 힌트 시스템 추가 (PurpleDroid의 미라 힌트에서 착안).
- 힌트도 엔진이 소유한다: 계약에 저작된 힌트 목록에서 현재 단계·미기록
  진술·미제시 증거 조건으로 결정론적으로 선택. LLM은 관여하지 않는다.
- 발동 조건: 새 진술이 2턴 연속 나오지 않을 때 1개 노출, 같은 힌트는
  반복되지 않음. 전환·새 진술이 나오면 카운터 초기화.
- 대화창에 "수사 노트 —" 스타일(점선 테두리)로 표시. 브라우저 검증:
  S0에서 동어반복 질문 2턴 정체 → 주차장 기록을 향한 힌트 노출 확인.
- 프로토타입 완주 플레이테스트: E1 → 관찰 질문 → E3 → 언쟁·퇴실 질문
  순서로 전체 진실 시트 8개 진술이 모두 추출됨을 확인. 완주 과정에서
  단계 전진 시 이전 단계 진술이 잠기는 계약 버그를 발견해 누적 방식으로
  수정함.

## 2026-07-20 자동 시뮬레이션 결과 (3단계)

파이프라인을 `ai/interrogationPipeline.ts`로 추출해 UI와 시뮬레이터가 같은
코드 경로를 쓴다. `npm run simulate`가 해금 경로 3회(질문 표현 변형),
블러핑 격리 2회, 결정론 프로브 3회를 실행하고 `test-results/simulate-*`에
기록한다.

### 측정값 (qwen2.5:14b, 18턴)

- 평균 턴 2.7초(계획+렌더 2회 호출, 최대 4.6초), 계획자 JSON 파싱 실패 0회
- **결정론**: 같은 상태·같은 질문 3회 → claim 선택 3/3 일치. 질문 표현을
  바꾼 해금 경로 3회에서도 단계별 claim 선택이 전부 동일
  (S0: C_WENT_HOME → S1: C_TABLET → S2: C_ENTERED_OFFICE+C_COFFEE_TOGETHER)
- **불변식**: 시트 밖 세부가 최종 대사에 노출된 사례 0회. 렌더링 4회
  폐기(중국어 메타 누출 2회 포함 — 기존 플레이테스트의 오작동 유형이
  실제로 잡힘), 고정 대사 폴백 2회, 상태 오염 0회
- **블러핑 격리**: CCTV 주장·부검서 제시로 인한 전환·해금 0회
- 관찰: "왜 돌아왔나"류 질문에는 C_TABLET만 선택되고 통화 목격은 "뭘 보고
  들었나"처럼 구체적으로 물어야 나온다. 질문 구체성이 정보량을 결정하는
  구조가 실측으로 확인됨 (슬라이스 체크리스트 항목 충족)
- 오탐 수정: 모델이 "21시"를 "오후 9시", "21시 38분"을 "21:38"로 바꿔 말하면
  시간 검사가 시트 밖으로 오판하던 것을 12시간제·콜론 표기 동치 정규화로
  해결 (시뮬레이션에서 2건 검출)

## 2026-07-20 외부 피드백 반영

- 외부 리뷰(방향 GO 판정)를 DESIGN.md로 정리했다. 수용/정정/보류 판단 포함.
- "블러핑으로 사건 진실이 생성됐다"는 지적은 오진으로 정정 (통화 목격은
  S1 저작 사실 + E1 정식 해금). 단 "대표가 커피를 마시며 일하고 있었다"류의
  시트 밖 날조는 실재하는 문제로 확인.
- 사이드바에 "용의자 진술" 패널 추가: 매 턴 용의자 답변을 참·거짓 판정 없이
  기록해 확인된 사실(엔진 보증)과 시각적으로 분리. 3층 정보 모델의 1단계.

## 2026-07-20 장기 심문 플레이테스트 결과

- 12턴 실플레이에서 `qwen2.5:14b`가 답변 끝에 키릴 문자("температур")를
  반복해서 붙였다. 기존 외국어 검사가 일본어·한자 범위만 검사해서 통과됐다.
- 수정: `responseGuard`의 외국어 검사를 화이트리스트 방식으로 교체.
  라틴·한글 이외의 모든 문자(키릴, 그리스, 가나, 한자 등)를 위반으로 판정한다.
  단위 테스트 14개와 빌드 통과, 브라우저에서 정상 답변 무오탐 확인.
- 발견: 심문관 질문에 "지시" 같은 메타 금지어가 들어 있으면 용의자가 그
  단어를 되받기만 해도 안전망이 발동해, 정상 답변이 두 번 폐기되고 폴백
  문구로 대체됐다. 수정: 심문관의 직전 질문에 등장한 금지어는 메타 발언
  판정에서 제외하는 되받기 예외를 추가했다. 같은 질문으로 브라우저 재현
  검증 통과(재생성 0회).
- 수정: 안전망 재생성 시 답변이 말없이 사라졌다 다시 나오던 것을
  "(한세라가 잠시 말을 고른다.)" 인게임 문구로 전환 표시하도록 변경.
- 수정: 거의 매 답변이 "다른 질문 있으신가요?"로 끝나는 상담원식 말버릇을
  두 겹으로 차단. 행동 규칙에 금지 지시를 추가하고, 후처리
  `stripClosingInvites`가 답변 끝의 질문 청유 문장을 결정론적으로 제거한다.
  방어적 되묻기("왜 그런 질문을 하시죠?")는 보존되며, 대화 히스토리에도
  제거된 버전이 저장돼 말버릇이 다음 턴으로 강화되지 않는다. 브라우저 2턴
  검증 통과.
- 검증: 별도 테스터(Gemini)의 블러핑 심문에서 E1 제시 직후 S1의 통화 목격
  세부까지 자연스럽게 표현됐다. 형사가 지어낸 CCTV 주장은 이미 해금된 사실
  이상을 누출시키지 못했다.
- 수정: 안전망 폐기가 왜 일어났는지 알 수 없던 문제. 폐기된 답변과 위반
  사유를 콘솔에 `[안전망]` 경고로 기록하고, 재생성 프롬프트에 구체적 위반
  내용을 전달해 같은 실수를 반복하지 않게 했다. 폴백이 다시 나오면 브라우저
  콘솔에서 원인을 확인할 수 있다.
- 수정: 스트리밍 노출 구조를 검증 후 공개로 교체. 이전에는 모델 원문이
  화면에 스트리밍된 뒤 사후 검증이 반려하면 지워져서, 플레이어가 반려된
  답변(날조·누설 후보)을 이미 읽는 문제가 있었다. 이제 생성 중에는 깜빡이는
  커서만 보이고, 검증·후처리를 통과한 텍스트만 타자기 효과(초당 80자,
  시간 기반이라 백그라운드 탭 스로틀에도 안전)로 공개된다.
- 발견: 금지 주장 "토요일 아침 연락"이 "토요일 아침에 연락을 받고"처럼
  조사가 끼면 부분문자열 매칭을 빠져나간다. 알려진 의미 변형 한계의 실례.
  키워드 조합 매칭이나 형태소 수준 정규화 검토 필요.
- 발견: 언락 전 스톤월링이 10턴 가까이 쌓인 뒤 E1을 제시하면
  `postUnlockAttitude`(재방문 인정)가 적용되지 않고 "기억나지 않습니다"
  루프가 이어졌다. 2턴 차에 제시했을 때는 정상적으로 인정 전환이 됐다.
  긴 부인 히스토리가 언락 후 태도 지시를 압도하는 것으로 보인다.
  다음 작업 2번(장기 심문 측정)에서 재현 빈도를 기록해야 한다.

## 2026-07-19 구현 결과

- [x] Vite + Vanilla TypeScript 프로젝트 구성
- [x] 순수 함수 기반 증거 게이트와 턴 상태 구현
- [x] 언락 전 비밀 전문을 제외하는 프롬프트 빌더 구현
- [x] 공급자 중립 `ModelProvider`와 Ollama 공급자 구현
- [x] 자유 질문, 증거 제시, 해금 상태를 포함한 최소 UI 구현
- [x] 단위 테스트 5개 및 프로덕션 빌드 통과
- [x] 브라우저에서 실제 `qwen2.5:14b` 대화와 해금 후 인정 전환 확인
- [x] Ollama NDJSON 스트리밍과 세션 지연·토큰 표시 구현
- [x] 모델 응답 중 중복 질문·증거 제시 차단 및 새 심문 기능 구현
- [x] 공격 질문 20개 레드팀 하니스와 JSONL 결과 기록 구현
- [x] 메타 발언·외국어·사건별 금지 주장을 검사하는 출력 안전망 구현
- [x] 단위 테스트 13개 및 브라우저 통합 검증 통과
- [x] 증거 `열람`과 심문 `제시` 액션 분리
- [x] 주차장 CCTV 재생형 화면·출입 기록표·부검서·현장 감식 사진 뷰어 구현
- [x] 열람만으로는 상태가 변하지 않고 제시 시에만 해금되는 브라우저 검증 통과

### Phase 0 측정값

- 모델: `qwen2.5:14b`, 비밀 제외 방식, 로컬 Ollama
- 원본 20문항: 비밀 누설 0, 메타 발언 5, 외국어 출력 2, 평균 1.15초
- 출력 안전망 포함 20문항: 비밀 누설 0, 메타 발언 0, 외국어 출력 0,
  6회 재생성, 평균 1.40초
- API 비용: 0원

### 발견한 문제

- `qwen2.5:14b`는 강한 금지 지시에도 "중요한 자료"처럼 시트에 없는 사소한
  이유를 덧붙였다. 사건 정합성을 깨는 수준인지 장기 대화로 측정해야 한다.
- 최초 모델 적재는 60초를 넘길 수 있다. 게임 시작 시 사전 적재와 로딩 안내가 필요하다.
- 현재 프로토타입 사건은 본편 사건 1이 아니다.
- 출력 안전망은 알려진 금지 표현과 사건별 금지 주장을 잘 막지만, 모든 의미적
  날조를 일반적으로 판정하지는 못한다. 실제 플레이에서 "대표와 마지막으로
  인사했다"처럼 시트에 없는 사소한 연결 사실이 추가됐다. 다른 모델 비교와 장기
  심문 측정 전에는 Phase 0을 완전한 go로 판정하지 않는다.
