# INTERRO 설계 방향

핵심 원칙 한 문장: **게임 엔진이 진실을 소유하고, LLM은 그 진실을 연기한다.**

방향 확정: "LLM과 자유 대화하는 심문 챗봇"이 아니라
"작가가 설계한 사건을 LLM 배우가 연기하는 증거 기반 추리게임"으로 만든다.

## 2026-07-20 외부 피드백 반영 판단

### 정정 — 사실관계가 다른 지적

- "플레이어의 CCTV 블러핑 때문에 통화 목격이라는 새 사건 정보가 즉석
  생성됐다"는 진단은 오진이다. 통화 목격은 fixture S1에 저작된 사실이고,
  해당 세션에서 블러핑 직전에 E1(주차장 기록)이 정식 제시되어 결정론적으로
  해금된 상태였다. 즉 시스템은 설계대로 동작했다. 다만 "플레이어 주장은
  정식 사실이 될 수 없다"는 원칙 자체는 유효하며 이미 엔진 원칙에 있다
  (AGENTS.md: 자유 텍스트 증거 주장은 제시로 판정하지 않는다).
- 단, 같은 세션에서 "대표가 커피를 마시며 일하고 있었다"처럼 시트에 없는
  목격 진술이 실제로 날조된 사례가 있다. 문제의 본질은 블러핑 수용이 아니라
  시트 밖 세부 날조이며, 아래 상태·검증 레이어가 해결책이다.

### 수용 — 사건 1 설계에 반영

1. **3층 정보 모델**: 확인된 사실(엔진 보증) / 진술(인물의 주장, 참·거짓
   미정) / 플레이어 추론(가설). UI와 상태 모두 이 구분을 따른다.
   프로토타입에는 "용의자 진술" 패널로 1단계 반영함.
2. **방어 단계 구조**: 용의자는 범인/무죄 이진 상태가 아니라
   실제_행적, 숨기는_비밀, 초기_거짓말, 단계별 방어(부인 → 부분 인정 →
   추가 인정 → 비밀 공개), 공개_조건(증거 게이트), 압박 성향을 가진다.
   현재 S1의 단일 게이트를 다단계로 일반화한다.
3. **거짓말 ≠ 범인**: 살인과 무관한 숨길 이유(내부고발, 자금 문제 등)를
   가진 무고한 용의자가 최소 1명 필요하다.
4. **블러핑 시스템**: 존재하지 않는 증거 주장은 `플레이어의 허풍`으로
   분류해 반응(의심, 반문, 동요)은 바뀌되 정식 사실은 절대 추가되지 않는다.
5. **승리 조건은 자백이 아니라 최종 보고서**: 범인·동기·방법·시간순서·핵심
   증거 3개를 제출하고 증거 사슬로 판정한다. 자백은 보너스 결과.
6. **핵심 사실 고정**: 같은 사건(같은 시드)이면 실제로 일어난 일은 항상
   같다. 달라져도 되는 것은 말투, 감정, 회피 순서, 문장 표현뿐이다.
7. **2단계 LLM 호출**: (1) 낮은 변동성으로 행동 선택(부인/부분 인정/역질문/
   침묵/공개)과 공개 가능한 사실 ID 결정 → (2) 결정된 내용만으로 대사 작성
   → (3) 검증기가 허용 밖 시간·인물·사건 추가 여부를 확인.
8. **컨텍스트 구성**: 전체 로그 대신 최근 몇 턴 + 구조화된 진술 목록을
   전달해 반복과 망각을 줄인다.
9. **모델 교체보다 상태 설계 우선**: qwen2.5:14b 수준에서 상태·검증
   레이어에 투자하는 것이 더 큰 모델을 찾는 것보다 우선이다.

### 수용 — UI 방향

- 진술 카드 + 증거 연결("이 진술과 충돌") UI가 다음 우선순위. 자동 모순
  알림은 쉬운 난이도로 남기고, 수동 연결을 상위 난이도로 검토.
- "확인된 새 사실" 해금 순간에 강한 보상 연출을 준다.
- 모델 선택 입력은 출시 빌드에서 개발자 옵션으로 숨긴다(개발 중에는 유지 —
  모델 비교 측정에 필요).

## 버티컬 슬라이스 (다음 마일스톤)

콘텐츠 추가나 모델 교체 전에 사건 하나를 완결된 게임으로 만든다.

- 용의자 3명, 증거 6~8개
- 용의자마다 3단계 이상의 거짓말/비밀 공개 구조
- 범행과 무관한 비밀을 가진 용의자 최소 1명
- 같은 사건에서 핵심 사실 고정
- 12회 심문 후 최종 사건 보고서 제출, 증거 사슬로 승패 판정

### 슬라이스 검증 체크리스트

- [ ] 존재하지 않는 증거를 말해도 정식 사실이 추가되지 않는가
- [ ] 같은 시드에서 실제 사건 내용이 항상 같은가
- [ ] 증거를 전부 제시하는 것보다 질문·제시 순서가 의미 있는가
- [ ] 자백 요구보다 구체적 시간·장소·행동 질문이 더 많은 정보를 얻는가
- [ ] 용의자가 자백하지 않아도 올바른 결론에 도달할 수 있는가
- [ ] 외부 플레이어가 설명 없이도 스스로 가설을 세우고 질문을 바꾸는가

## 최우선 구현 항목

**상태·검증 레이어**: 플레이어의 주장, 용의자의 진술, 검증된 증거를
분리하고, LLM이 허용된 사건 사실 밖으로 나가지 못하게 하는 층.
이 층이 잡혀야 좋은 순간들이 우연한 출력이 아니라 재현 가능한
게임플레이가 된다.
