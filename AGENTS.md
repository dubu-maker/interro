# INTERRO 개발 원칙

- 문서, UI 텍스트, 커밋 메시지는 한국어로 작성한다. 코드 식별자는 영어를 쓴다.
- 프레임워크 없이 Vite + Vanilla TypeScript를 사용하고 `strict`를 유지한다.
- LLM은 인물의 표현만 담당한다. 게임 상태는 UI 액션과 결정론적 엔진만 변경한다.
- 언락되지 않은 비밀은 모델 프롬프트에 포함하지 않는다.
- 자유 텍스트에 적힌 증거 주장은 실제 증거 제시로 판정하지 않는다.
- 모델 공급자에 종속되지 않도록 `ModelProvider` 경계를 유지한다.
- `src/cases/`는 스포일러 영역이다.
- 변경 후 `npm test`와 `npm run build`를 통과시킨다.
- 세션 종료 시 `HANDOFF.md`를 갱신한다.
